name: Build VS Code AppImage (Insiders)

on:
  workflow_dispatch:
  schedule:
    # Run every 6 hours to check upstream for a new Insiders archive.
    # The workflow only builds when a new upstream SHA is detected.
    - cron: "0 */6 * * *"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Install AppImage build dependencies
        run: |
          sudo apt update
          sudo apt install -y curl tar desktop-file-utils

      - name: Resolve VS Code Insiders archive and decide whether to build
        id: check
        run: |
          bash scripts/resolve-vscode-release.sh \
            insiders \
            vscode-appimage-insiders-sha \
            "$GITHUB_OUTPUT" \
            "${{ github.event_name }}"

      - name: Skip build (no new Insiders archive)
        if: steps.check.outputs.should_build != 'true'
        run: echo "No new Insiders archive detected. Skipping build steps."

      - name: Build AppImage
        if: steps.check.outputs.should_build == 'true'
        run: |
          bash scripts/build-appimage.sh \
            insiders \
            "${{ steps.check.outputs.url }}" \
            "${{ steps.check.outputs.version }}" \
            "vscode-appimage-insiders.AppImage"
          ls -lh vscode-appimage-insiders.AppImage

      - name: Upload AppImage artifact
        if: steps.check.outputs.should_build == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: vscode-appimage-insiders
          path: vscode-appimage-insiders.AppImage

      - name: Tag processed Insiders archive
        if: steps.check.outputs.build_needed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "${{ steps.check.outputs.tag }}"
          git push origin "refs/tags/${{ steps.check.outputs.tag }}" || true

      - name: Create GitHub release (Insiders AppImage)
        if: steps.check.outputs.build_needed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ steps.check.outputs.tag }}" \
            "vscode-appimage-insiders.AppImage" \
            --title "VS Code Insiders AppImage v${{ steps.check.outputs.version }}" \
            --notes "Automated AppImage build for VS Code Insiders v${{ steps.check.outputs.version }} (SHA ${{ steps.check.outputs.sha }})."
